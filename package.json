{
  "name": "rug-porter",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "prepare": "svelte-kit sync || echo ''",
    "deploy": "npm run build && touch build/.nojekyll && npx gh-pages -d build"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^7.0.1",
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.60.1",
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "gh-pages": "^6.3.0",
    "svelte": "^5.55.7",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "fast-glob": "^3.3.3"
  }
}
